# PS2 — Correlation & Explainability Layer

- `fraud_detection/` — PaySim-based fraud pattern models
- `correlation_engine/` — joins PS1 signals + telemetry + transactions
- `explainability/` — structured reason generation for every alert

See docs/ARCHITECTURE.md and docs/FEATURES.md for full spec.
